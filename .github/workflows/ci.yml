name: CI

on:
  push:
    branches:
    - main

jobs:
  test:
    runs-on: ubuntu_latest

    steps:
    - name: checkout repository
      uses: actions/checkout@v4

    - name: runs python
      run: python --version

    - name: run application
      run: pyhton app.py